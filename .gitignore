# OS
.DS_Store
Thumbs.db

# Logs
*.log
npm-debug.log*
/tmp/

# Env (NUNCA commitear secretos)
.env
.env.local

# Composer vendor (se instala con composer install, no se versiona)
vendor/

# Figma
figma/*.fig

# Apache
.htaccess.bak

# Reportes PDF generados (los .docx sí se versionan en reportes/)
reportes/*.pdf

# Capturas locales (evidencia ya incrustada en los .docx, no se versionan)
imagenes/
